# Core Django Framework
Django==5.1.3
django-environ==0.11.2  # Secure environment variable management

# Security & Authentication
argon2-cffi==23.1.0  # Argon2 password hashing (OWASP recommended)
django-otp==1.5.4  # Core OTP framework
qrcode==7.4.2  # QR code generation for TOTP setup
django-two-factor-auth[phonenumbers]==1.16.0  # Complete 2FA solution
django-axes==6.5.2  # Brute force protection & rate limiting

# Database Drivers
psycopg2-binary==2.9.9  # PostgreSQL adapter
mysqlclient==2.2.4  # MySQL adapter (alternative to psycopg2)

# API & CORS
djangorestframework==3.15.2  # REST API framework
django-cors-headers==4.4.0  # CORS security headers
djangorestframework-simplejwt==5.4.0  # JWT authentication for API

# Content Management
Pillow==10.4.0  # Image processing with security patches
django-ckeditor-5==0.2.13  # Rich text editor with XSS protection
bleach==6.1.0  # HTML sanitization to prevent XSS
django-csp==3.8  # Content Security Policy headers

# Security Tools & Analysis
bandit==1.7.10  # Python AST security linter
semgrep==1.97.0  # Static analysis for security patterns
safety==3.2.8  # Dependency vulnerability scanner
django-defender==0.9.8  # Login attempt monitoring

# Development & Testing
pre-commit==3.8.0  # Git hooks for code quality
pytest==8.3.3  # Testing framework
pytest-django==4.9.0  # Django test integration
pytest-cov==5.0.0  # Coverage reporting
factory-boy==3.3.1  # Test data factories
django-debug-toolbar==4.4.6  # Development debugging (disable in prod)

# Production Server
gunicorn==23.0.0  # WSGI HTTP Server
whitenoise==6.7.0  # Static file serving with security headers

# Additional Security
django-ratelimit==4.1.0  # Rate limiting for views
django-honeypot==1.2.1  # Bot trap for forms
cloudflare-turnstile==0.1.2  # Cloudflare Turnstile CAPTCHA integration
django-ssl-redirect==2.1.1  # Force HTTPS in production

# Monitoring & Logging
django-structlog==8.1.0  # Structured logging for security events
sentry-sdk==2.18.0  # Error tracking and monitoring